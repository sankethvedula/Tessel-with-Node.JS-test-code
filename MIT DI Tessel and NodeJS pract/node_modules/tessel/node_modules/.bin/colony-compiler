#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../colony-compiler/bin/colony-compiler.js" "$@"
  ret=$?
else 
  node  "$basedir/../colony-compiler/bin/colony-compiler.js" "$@"
  ret=$?
fi
exit $ret
